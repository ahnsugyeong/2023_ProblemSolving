// baekjoon 14888

#include <bits/stdc++.h>

using namespace std;

int main() {
    int N;
    vector<int> A;
    vector<string> op;


    cin >> N;
    for (int i = 0; i < N; i++) {
        int tmp;
        cin >> tmp;
        A.push_back(tmp);
    }
    for (int i = 0; i < N; i++) {
        string tmp;
        cin >> tmp;
        op.push_back(tmp);
    }


}